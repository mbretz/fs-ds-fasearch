{
  "name": "icons",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build:manifest": "node scripts/build-manifest.ts",
    "build:svgr": "svgr --config-file svgr.config.cjs --out-dir src/generated source",
    "build": "pnpm build:manifest && pnpm build:svgr",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.2.7"
  },
  "devDependencies": {
    "@svgr/cli": "^8.1.0",
    "@types/node": "^24.11.1",
    "@types/react": "^19.2.17",
    "svgo": "^4.0.1",
    "typescript": "^6.0.3"
  }
}
